name: fz_animations
description: Animation utilities for Flutter including custom transitions.
publish_to: 'none'
version: 0.1.0
environment:
  sdk: "^3.9.0"
dependencies:
  flutter:
    sdk: flutter
  sliver_tools: ^0.2.12

dependency_overrides:
  intl: ^0.20.2
  archive: ^4.0.7
