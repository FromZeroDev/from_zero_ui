name: fz_animated_switcher_image
description: AnimatedSwitcher that captures static snapshots of outgoing children, avoiding rebuilds during exit animations.
publish_to: 'none'
version: 0.1.0
environment:
  sdk: "^3.9.0"
dependencies:
  flutter:
    sdk: flutter
  sliver_tools: ^0.2.12

dependency_overrides:
  intl: ^0.20.2
  archive: ^4.0.7

dev_dependencies:
  flutter_lints: ^5.0.0
