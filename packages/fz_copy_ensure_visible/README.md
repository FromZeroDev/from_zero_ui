# fz_copy_ensure_visible

Ensures a focused widget is visible in the scroll viewport, copied and customized from Flutter.

## Usage

```dart
import 'package:fz_copy_ensure_visible/fz_copy_ensure_visible.dart';

// Used internally by DAO fields and expansion tiles automatically.
```

Internal utility — rarely used directly.
