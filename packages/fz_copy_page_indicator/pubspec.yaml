name: fz_copy_page_indicator
description: Arrow page indicator widget (copied from Flutter).
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.9.0"

dependencies:
  flutter:
    sdk: flutter

  page_view_indicators: ^2.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
