# fz_copy_page_indicator

> **Primarily for internal use** by `fz_export`'s PDF page indicator — rarely needed directly.

An arrow-based page indicator copied from Flutter.

**Difference from Flutter's built-in**: exists as a standalone widget because the original is internal to the Flutter framework and not publicly accessible.
