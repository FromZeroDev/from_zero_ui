name: fz_tooltip
description: Material tooltip widget (copied and customized from Flutter).
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.9.0"

dependencies:
  flutter:
    sdk: flutter

  fz_platform:
    path: ../fz_platform
  fz_scrollbar:
    path: ../fz_scrollbar


dev_dependencies:
  flutter_lints: ^5.0.0
