name: fz_router
description: GoRouter extensions for FromZero UI.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.9.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: 2.0.0-dev.9
  go_router: 10.0.0

  fz_scaffold:
    path: ../fz_scaffold

dev_dependencies:
  flutter_lints: ^5.0.0
