name: fz_copy_sticky_header
description: Sliver sticky header widget (copied from Flutter).
publish_to: 'none'
version: 0.1.0
environment:
  sdk: "^3.9.0"
dependencies:
  flutter:
    sdk: flutter
  sliver_tools: ^0.2.12
  value_layout_builder: ^0.5.0

dependency_overrides:
  intl: ^0.20.2
  archive: ^4.0.7
