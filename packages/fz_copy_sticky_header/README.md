# fz_copy_sticky_header

> **Primarily for internal use** by `fz_table` — rarely needed directly.

A sliver sticky header implementation copied from Flutter.

**Difference from Flutter's built-in**: exists as a standalone widget because the original `SliverStickyHeader` is internal to the Flutter framework and not publicly accessible.
