name: fz_ui_scale
description: UI scale picker widget for FromZero UI.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.9.0"

dependencies:
  flutter:
    sdk: flutter

  fz_theme:
    path: ../fz_theme


dev_dependencies:
  flutter_lints: ^5.0.0
